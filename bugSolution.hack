function foo(x: int): int {
  return x + 1;
}

function bar(x: int): int {
  return foo(x) * 2;
}

function baz(x: int): int {
  return bar(x) - 1;
}

function main(): void {
  $result = baz(5);
  echo $result;
}

// Expected output: 11
// Actual output: 11